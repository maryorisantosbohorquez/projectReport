<UserControl x:Class="ProjectReport.Views.Geometry.WellTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Background="{StaticResource WhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="{StaticResource GrayBrush}" CornerRadius="4" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Well:" FontSize="14" FontWeight="SemiBold" 
                          VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource BlackBrush}"/>
                <TextBox Grid.Column="1" x:Name="WellNameTextBox" Text="{Binding WellName, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,20,0" Height="30"/>
                
                <TextBlock Grid.Column="2" Text="Report Number:" FontSize="14" FontWeight="SemiBold" 
                          VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource BlackBrush}"/>
                <TextBox Grid.Column="3" x:Name="ReportNumberTextBox" Text="{Binding ReportNumber, UpdateSourceTrigger=PropertyChanged}"
                        Height="30"/>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Add Well Test Button -->
                <Button Grid.Row="0" Content="Add Well Test" 
                       Style="{StaticResource DefaultButton}" 
                       HorizontalAlignment="Right" Margin="0,0,0,15"
                       Click="AddWellTest_Click"/>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" x:Name="WellTestDataGrid" 
                         ItemsSource="{Binding WellTests}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="True"
                         SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Section" Binding="{Binding Section}" Width="150"/>
                        <DataGridTemplateColumn Header="Component Type" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ComponentType}" VerticalAlignment="Center" Padding="10,8"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox SelectedItem="{Binding ComponentType, UpdateSourceTrigger=PropertyChanged}">
                                        <sys:String>Liner</sys:String>
                                        <sys:String>Casing</sys:String>
                                        <sys:String>Tubing</sys:String>
                                    </ComboBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Dropdown Test Type" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TypeString}" 
                                              VerticalAlignment="Center" Padding="10,8"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.WellTestTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                             SelectedItem="{Binding TypeString, UpdateSourceTrigger=PropertyChanged}"
                                             DisplayMemberPath="."/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Test Value (ppb)" Binding="{Binding TestValue, StringFormat=F2}" Width="140"/>
                        
                        <!-- Delete Button Column -->
                        <DataGridTemplateColumn Header="Actions" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" 
                                           Style="{StaticResource DefaultButton}"
                                           Background="#DC3545"
                                           Padding="8,4"
                                           Click="DeleteWellTest_Click"
                                           Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
