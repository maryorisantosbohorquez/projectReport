<UserControl x:Class="ProjectReport.Views.Geometry.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource WhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="{StaticResource GrayBrush}" CornerRadius="4" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Well:" FontSize="14" FontWeight="SemiBold" 
                          VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource BlackBrush}"/>
                <TextBox Grid.Column="1" x:Name="WellNameTextBox" Text="{Binding WellName, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,20,0" Height="30"/>
                
                <TextBlock Grid.Column="2" Text="Report Number:" FontSize="14" FontWeight="SemiBold" 
                          VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource BlackBrush}"/>
                <TextBox Grid.Column="3" x:Name="ReportNumberTextBox" Text="{Binding ReportNumber, UpdateSourceTrigger=PropertyChanged}"
                        Height="30"/>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Volume Summary Cards -->
                <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="4" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="10,0">
                            <TextBlock Text="Total Wellbore Volume" FontSize="11" 
                                      Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalWellboreVolume, StringFormat='{}{0:F2} bbl'}" 
                                      FontSize="20" FontWeight="Bold" 
                                      Foreground="#333333"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="Total Drill String Volume" FontSize="11" 
                                      Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalDrillStringVolume, StringFormat='{}{0:F2} bbl'}" 
                                      FontSize="20" FontWeight="Bold" 
                                      Foreground="#333333"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Margin="10,0">
                            <TextBlock Text="Total Annular Volume" FontSize="11" 
                                      Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalAnnularVolume, StringFormat='{}{0:F2} bbl'}" 
                                      FontSize="20" FontWeight="Bold" 
                                      Foreground="#DC3545"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="3" Margin="10,0">
                            <TextBlock Text="Total Circulation Volume" FontSize="11" 
                                      Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalCirculationVolume, StringFormat='{}{0:F2} bbl'}" 
                                      FontSize="20" FontWeight="Bold" 
                                      Foreground="#28A745"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Visual Scheme and Annular Details Side by Side -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Visual Scheme (Left) -->
                    <Border Grid.Column="0" Background="#1A2332" CornerRadius="4" Margin="0,0,10,0" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Visual Scheme" 
                                      FontSize="14" FontWeight="SemiBold" 
                                      Foreground="White" Margin="0,0,0,10"/>
                            
                            <Canvas Grid.Row="1" x:Name="VisualSchemeCanvas" 
                                   Background="#1A2332" 
                                   MinHeight="400"/>
                        </Grid>
                    </Border>

                    <!-- Annular Volume Details (Right) -->
                    <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Annular Volume Details (por Sección / Componente)" 
                                      FontSize="13" FontWeight="SemiBold" 
                                      Foreground="#333333" Margin="0,0,0,10" TextWrapping="Wrap"/>
                            
                            <DataGrid Grid.Row="1" 
                                     ItemsSource="{Binding AnnularVolumeDetails}"
                                     AutoGenerateColumns="False"
                                     CanUserAddRows="False"
                                     CanUserDeleteRows="False"
                                     IsReadOnly="True"
                                     HeadersVisibility="Column"
                                     GridLinesVisibility="Horizontal"
                                     RowBackground="White"
                                     AlternatingRowBackground="#F8F9FA"
                                     BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                                    <DataGridTextColumn Header="Section/Component" Binding="{Binding Name}" Width="*" MinWidth="100"/>
                                    <DataGridTextColumn Header="Wellbore ID (in)" Binding="{Binding WellboreID, StringFormat=F2}" Width="90"/>
                                    <DataGridTextColumn Header="DS OD (in)" Binding="{Binding DrillStringOD, StringFormat=F2}" Width="80"/>
                                    <DataGridTextColumn Header="Depth Range (ft)" Binding="{Binding DepthRange}" Width="120"/>
                                    <DataGridTextColumn Header="Annular Vol (bbl)" Binding="{Binding AnnularVolume, StringFormat=F2}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#DC3545"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>

